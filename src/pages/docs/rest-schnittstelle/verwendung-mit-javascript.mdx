---
title: 'Datenguide | REST-Schnittstelle | Verwendung mit Javascript'
layout: DocsPage
---

# Verwendung mit Javascript

Im folgenden findest du den Code, um die REST-Schnittstelle mit Javascript abzufragen. Eine Schritt-für-Schritt
Anleitung wie die URL für die Abfrage der Schnittstelle aufgebaut sein muss, findest du auf [INTERNERT_LINK].
##Abfrage der REST-Schnittstelle##

###################### Javascript einfügen

```
```
JSON
format=json

Gibt die Daten (key: data) als JSON (response mimetype: application/json) zurück samt metadaten (key: schema) 
zur Tabellenstruktur.

```
```
